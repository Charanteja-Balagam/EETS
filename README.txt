Install the required dependencies:

Go the path where application files exists (EETS\venv), open CLI and give the command.
"pip install -r dependencies.txt"

requirements:
Flask
matplotlib


Go to the path where application files exists (EETS\venv) and open CLI and run the command "python eets.py"

The application will be accessible at `http://localhost:5000` in your web browser.